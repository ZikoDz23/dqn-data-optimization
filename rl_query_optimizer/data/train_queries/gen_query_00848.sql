SELECT count(*) FROM name, cast_info, aka_name, role_type, title, movie_info, kind_type WHERE cast_info.person_id = name.id AND aka_name.person_id = name.id AND cast_info.role_id = role_type.id AND cast_info.movie_id = title.id AND movie_info.movie_id = title.id AND title.kind_id = kind_type.id;