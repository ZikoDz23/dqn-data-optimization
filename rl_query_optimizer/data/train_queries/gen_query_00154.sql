SELECT count(*) FROM name, cast_info, title, aka_name, role_type WHERE cast_info.person_id = name.id AND cast_info.movie_id = title.id AND aka_name.person_id = name.id AND cast_info.role_id = role_type.id;