SELECT count(*) FROM char_name, cast_info, title, name, movie_companies WHERE cast_info.person_role_id = char_name.id AND cast_info.movie_id = title.id AND cast_info.person_id = name.id AND movie_companies.movie_id = title.id;