SELECT count(*) FROM movie_info, title, kind_type, movie_companies, company_type WHERE movie_info.movie_id = title.id AND title.kind_id = kind_type.id AND movie_companies.movie_id = title.id AND movie_companies.company_type_id = company_type.id;