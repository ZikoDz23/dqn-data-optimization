SELECT count(*) FROM movie_info, title, movie_companies, kind_type, company_name WHERE movie_info.movie_id = title.id AND movie_companies.movie_id = title.id AND title.kind_id = kind_type.id AND movie_companies.company_id = company_name.id;