SELECT count(*) FROM char_name, cast_info, name, aka_name, title WHERE cast_info.person_role_id = char_name.id AND cast_info.person_id = name.id AND aka_name.person_id = name.id AND cast_info.movie_id = title.id;