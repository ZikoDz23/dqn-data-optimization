SELECT count(*) FROM name, cast_info, aka_name, title, role_type WHERE cast_info.person_id = name.id AND aka_name.person_id = name.id AND cast_info.movie_id = title.id AND cast_info.role_id = role_type.id;