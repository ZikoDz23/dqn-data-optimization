SELECT count(*) FROM movie_info, info_type, title, cast_info, name WHERE movie_info.info_type_id = info_type.id AND movie_info.movie_id = title.id AND cast_info.movie_id = title.id AND cast_info.person_id = name.id;