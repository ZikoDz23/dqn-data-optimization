SELECT count(*) FROM cast_info, name, aka_name, title, movie_info, info_type WHERE cast_info.person_id = name.id AND aka_name.person_id = name.id AND cast_info.movie_id = title.id AND movie_info.movie_id = title.id AND movie_info.info_type_id = info_type.id;