SELECT count(*) FROM title, cast_info, movie_keyword, kind_type, name WHERE cast_info.movie_id = title.id AND movie_keyword.movie_id = title.id AND title.kind_id = kind_type.id AND cast_info.person_id = name.id;