SELECT count(*) FROM name, cast_info, aka_name, title, movie_companies WHERE cast_info.person_id = name.id AND aka_name.person_id = name.id AND cast_info.movie_id = title.id AND movie_companies.movie_id = title.id;