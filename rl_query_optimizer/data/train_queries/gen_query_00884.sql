SELECT count(*) FROM name, cast_info, role_type, title, aka_name WHERE cast_info.person_id = name.id AND cast_info.role_id = role_type.id AND cast_info.movie_id = title.id AND aka_name.person_id = name.id;