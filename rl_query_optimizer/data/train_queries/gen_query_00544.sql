SELECT count(*) FROM info_type, movie_info, title, cast_info, movie_companies WHERE movie_info.info_type_id = info_type.id AND movie_info.movie_id = title.id AND cast_info.movie_id = title.id AND movie_companies.movie_id = title.id;